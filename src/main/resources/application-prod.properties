spring.datasource.url=${SUPABASE_DB_URL:jdbc:postgresql://aws-1-eu-central-1.pooler.supabase.com:6543/postgres?prepareThreshold=0}
spring.datasource.username=${SUPABASE_DB_USER:postgres.veizbtzugssszhxabzrv}
spring.datasource.password=${SUPABASE_DB_PASSWORD}
spring.datasource.driver-class-name=org.postgresql.Driver
spring.datasource.hikari.maximum-pool-size=${DB_MAX_POOL_SIZE:10}
spring.datasource.hikari.minimum-idle=${DB_MIN_IDLE:2}
spring.datasource.hikari.connection-timeout=${DB_CONNECTION_TIMEOUT_MS:30000}
spring.datasource.hikari.validation-timeout=${DB_VALIDATION_TIMEOUT_MS:5000}
spring.datasource.hikari.idle-timeout=${DB_IDLE_TIMEOUT_MS:300000}
spring.datasource.hikari.max-lifetime=${DB_MAX_LIFETIME_MS:900000}
spring.datasource.hikari.keepalive-time=${DB_KEEPALIVE_TIME_MS:300000}
spring.threads.virtual.enabled=true
spring.jpa.hibernate.ddl-auto=validate
spring.flyway.enabled=true
spring.flyway.locations=classpath:db/migration
app.concurrency.stock-locking=pessimistic-write
app.security.h2-console-enabled=false
app.jwt.secret=${JWT_SECRET}
app.jwt.expiration-minutes=${JWT_EXPIRATION_MINUTES:60}
logging.level.root=INFO
logging.level.com.king_sparkon_tracker.backend=INFO
app.cors.allowed-origins=${CORS_ALLOWED_ORIGINS:}
app.cors.allowed-origin-patterns=${CORS_ALLOWED_ORIGIN_PATTERNS:}
springdoc.api-docs.enabled=${SPRINGDOC_API_DOCS_ENABLED:false}
springdoc.swagger-ui.enabled=${SPRINGDOC_SWAGGER_UI_ENABLED:false}
app.mail.enabled=${APP_MAIL_ENABLED:true}
app.mail.from=${APP_MAIL_FROM:no-reply@kingsparkon-tracker.com}
app.contact.notification-email=${CONTACT_NOTIFICATION_EMAIL:${APP_MAIL_FROM:no-reply@kingsparkon-tracker.com}}
spring.mail.host=${MAIL_HOST:smtp-relay.brevo.com}
spring.mail.port=${MAIL_PORT:587}
spring.mail.username=${MAIL_USERNAME}
spring.mail.password=${MAIL_PASSWORD}
spring.mail.properties.mail.smtp.auth=${MAIL_SMTP_AUTH:true}
spring.mail.properties.mail.smtp.starttls.enable=${MAIL_SMTP_STARTTLS_ENABLE:true}
spring.mail.properties.mail.smtp.starttls.required=${MAIL_SMTP_STARTTLS_REQUIRED:true}
spring.mail.properties.mail.smtp.connectiontimeout=${MAIL_SMTP_CONNECTION_TIMEOUT_MS:5000}
spring.mail.properties.mail.smtp.timeout=${MAIL_SMTP_TIMEOUT_MS:5000}
spring.mail.properties.mail.smtp.writetimeout=${MAIL_SMTP_WRITE_TIMEOUT_MS:5000}

spring.ai.model.chat=${SPRING_AI_CHAT_PROVIDER:openai}
spring.ai.model.embedding=${SPRING_AI_EMBEDDING_PROVIDER:openai}
spring.ai.openai.api-key=${OPENAI_API_KEY}
spring.ai.openai.chat.model=${OPENAI_CHAT_MODEL:gpt-4o-mini}
spring.ai.openai.chat.temperature=${OPENAI_CHAT_TEMPERATURE:0.3}
spring.ai.openai.embedding.model=${OPENAI_EMBEDDING_MODEL:text-embedding-3-small}
spring.ai.openai.embedding.dimensions=${OPENAI_EMBEDDING_DIMENSIONS:768}
spring.ai.vectorstore.pgvector.dimensions=${AI_VECTORSTORE_DIMENSIONS:768}
app.ai.chat.provider=${SPRING_AI_CHAT_PROVIDER:openai}
app.ai.chat.model=${APP_AI_CHAT_MODEL:${OPENAI_CHAT_MODEL:gpt-4o-mini}}

stripe.secret-key=${STRIPE_SECRET_KEY}
stripe.webhook-secret=${STRIPE_WEBHOOK_SECRET:}
stripe.success-url=${STRIPE_SUCCESS_URL:http://localhost:3000/dashboard/owner?billing=stripe-success}
stripe.cancel-url=${STRIPE_CANCEL_URL:http://localhost:3000/dashboard/owner?billing=stripe-cancelled}

paypal.enabled=${PAYPAL_ENABLED:false}
paypal.base-url=${PAYPAL_BASE_URL:https://api-m.sandbox.paypal.com}
paypal.client-id=${PAYPAL_CLIENT_ID:paypal-disabled-client-id}
paypal.client-secret=${PAYPAL_CLIENT_SECRET:paypal-disabled-client-secret}
paypal.webhook-id=${PAYPAL_WEBHOOK_ID:paypal-disabled-webhook-id}
paypal.return-url=${PAYPAL_RETURN_URL:${FRONTEND_LOGIN_URL:https://king-sparkon-tracker.vercel.app}/billing/return}
paypal.cancel-url=${PAYPAL_CANCEL_URL:${FRONTEND_LOGIN_URL:https://king-sparkon-tracker.vercel.app}/billing/cancel}
paypal.billing.plus-monthly-plan-id=${PAYPAL_BILLING_PLUS_MONTHLY_PLAN_ID:paypal-disabled-plus-monthly}
paypal.billing.plus-yearly-plan-id=${PAYPAL_BILLING_PLUS_YEARLY_PLAN_ID:paypal-disabled-plus-yearly}
paypal.billing.pro-monthly-plan-id=${PAYPAL_BILLING_PRO_MONTHLY_PLAN_ID:paypal-disabled-pro-monthly}
paypal.billing.pro-yearly-plan-id=${PAYPAL_BILLING_PRO_YEARLY_PLAN_ID:paypal-disabled-pro-yearly}
